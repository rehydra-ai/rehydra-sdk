/**
 * Rehydra OpenCode Plugin
 * Anonymizes secrets in LLM context via OpenCode's native plugin hooks.
 */

export { createRehydraPlugin, default } from "./plugin.js";
export type { RehydraPluginOptions } from "./types.js";
